import {StyleSheet} from 'react-native';
import {fonts} from '../../assets/fonts';

export const HeaderContentStyle = StyleSheet.create({
  text: {
    // color: 'red',
    fontFamily: fonts.primary.bold,
    color: '#000',
    fontSize: 15,
  },
});
